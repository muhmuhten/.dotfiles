#!/bin/execlineb -S0
background {
  elglob -0 u2d .user2.d/*
  forx -p u2 { $u2d }
  import -iu u2
  $u2 $@
}

getpid pid import -iu pid
define dest /tmp/svscan${pid}
if { mkdir $dest }
if { pipeline { tar cC .s6 . } tar xC $dest }

emptyenv -p
s6-svscan -St0 $dest
